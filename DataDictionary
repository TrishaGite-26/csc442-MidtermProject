Data Dictionary

Drugs Dataset:
remove the cols: if their is a year then remove month, remove all rates

Heart Disease and Hospitals Dataset: 
remove the cols: remove first and last names 


The columns being features of the data, and it may also contain descriptions of 
how the data is structured. Yes, sometimes it also contains information on why it was collected.
